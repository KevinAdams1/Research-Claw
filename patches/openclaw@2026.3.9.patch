# Research-Claw branding patch for OpenClaw 2026.3.9
# ~20 lines across 7 files: CLI name, process title, system prompt product name,
# update URL, launchd/systemd service names.
#
# This is a STUB patch. Generate the real patch with:
#   bash scripts/apply-branding.sh
#
# The actual patch will modify:
# 1. src/cli/index.ts          — CLI command name: openclaw → research-claw
# 2. src/cli/version.ts        — Product name in version output
# 3. src/runtime.ts            — Process title
# 4. src/agents/system.ts      — Product name in system prompt
# 5. src/update/check.ts       — Update check URL
# 6. src/daemon/launchd.ts     — macOS service label
# 7. src/daemon/systemd.ts     — Linux service name
